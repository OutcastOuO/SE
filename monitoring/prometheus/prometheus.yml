global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: vms-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - vms-api:8000
